# Registro de Organización y Pair Programming
## 1. Integrantes del Equipo
* **Alumno A (Identificador):** Siarhei Lukashou
* **Alumno B (Identificador):** Elias Perez Arroyo
## 2. Bitácora de Turnos de Programación
* **Turno 1 (Conductor Alumno A):** Se ha creado proyecto y repositorio. La estructura de la clase `Mensaje.java` con todos los atributos, constructores `Mensaje()` y `Mensaje(String  usuario, String  ip, String  texto)`, `getters()`, desarollo del metodo `esValida(String  ip)` cual comprueba si la IP es valida atraves de una expresion regular `^(?:(?:25[0-5]|2[0-4]\\d|1?\\d{1,2})(?:\\.(?!$)|$)){4}`.
* **Turno 2 (Conductor Alumno B):** Se implementaron los algoritmos del método `reverse()` para invertir las letras de cada palabra, `encriptar()` que transforma las letras 3 valores a la derecha y en caso de no haber letras a la derecha da la vuelta al abecedario y `desencriptar()`  que transforma las letras 3 valores a la izquierda. <br><br>Se ha creado la clase `Cola.java` con una estructura dinámica tipo `LinkedList` que actuará como una cola como único atributo, se ha añadido el constructor por defecto que crea la estructura vacía y se han añadido los métodos `meterEnCola()` para meter un objeto tipo `Object` en última posición de la cola, `sacarDeCola()` para sacar y eliminar el primero y en caso de no haber mensajes mostrar un mensaje por pantalla y `esColaVacia()` para verificar si está vacía o no la cola. 
* **Turno 3 (Conductor Alumno A):** Se codificó la estructura del
menú por consola en la clase `Mensajeria` y las opciones 1 y 2...
* **Turno 4 (Conductor Alumno B):** Se programó la lógica de
recepción de la cola y el control de salida con confirmación...
## 3. Evidencia del Trabajo Síncrono
![Screenshot de llamada en Discord](https://i.ibb.co/4n3H7mc7/image.png)

